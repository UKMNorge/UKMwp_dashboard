<style>
.img {
    background: #ddd;
    border: 2px solid #0073aa;
    width: 1.5em;
    height: 1.5em;
    text-align: center;
    vertical-align: middle;
    padding-top: 0.15em;
    font-size: 3em;
}
.img > .dashicons {
    font-size: 1em;
    width: 1em;
    height: 1em;
    line-height: 1em;
    color: #0073aa;
}
</style>
<div class="card" id="currentUser">
    <div class="img img-circle" style="float:right;">
        <div class="dashicons dashicons-admin-users"></div>
    </div>
    <b>{{ current_user.display_name }}</b>
    {% if current_user.user_phone is not empty and current_user.user_phone > 0 %}
        <br />
        📱 {{ current_user.user_phone }}
    {% endif %}
	<br />
	✉️ {{ current_user.user_email }}


    {% if current_user.user_phone is empty or current_user.user_phone == 0 %}
        <p class="mt-3">Vi vil veldig gjerne <a href="profile.php">vite mobilnummeret ditt</a>.</p>
    {% endif %}
    <div class="clearfix" style="margin-bottom: 1.8em;"></div>
    <a href="profile.php" class="btn btn-block btn-ukm" >rediger informasjon</a>
</div>