{% if post is defined %}
	{% embed "post.html.twig" %}{% endembed %}
{% else %}
	{% if page == 1 %}
		<div class="clearfix"></div>
		<div class="header-dashboard" style="margin: 0px 0px 1em -20px;">VELKOMMEN TIL ARRANGØRSYSTEMET!</div>
		<div class="clearfix"></div>
	{% endif %}

	<div class="container-fluid">
		{% if page == 1 %}
			<div class="row">
				<div class="col-xs-12 col-md-8">
					{% if fylke_eller_kommunebruker %}
						{% include "user/intro.html.twig" %}
					{% else %}
						{% include "user/arrangementer.html.twig" %}
					{% endif %}
					{% include "user/news.html.twig" %}
				</div>
				<div class="col-xs-12 col-md-4">
					{% include "user/kurs.html.twig" %}
				</div>
				<div class="col-xs-12 col-md-4">
					{% include "user/current_user.html.twig" %}
				</div>
			</div>
		{% else %}
			<div class="row">
				<div class="col-xs-12 col-md-8">
					{% include "user/news.html.twig" %}
				</div>
			</div>
		{% endif %}
    </div>
{% endif %}